
<h1>Book detail</h1>
<table>
	<tbody>
		<tr>
			<td></td>
			<td><%= @book.title %></td>
			<td><%= @book.body %></td>
			<td>
			   <span class="button">
			 	<%= link_to "edit", edit_book_path(@book) %>
		 	   </span>
     		   <span class="button">
                <% if @book.user == current_user %>
			 	<%= link_to "Destroy", book_path(@book.id), method: :delete,"date-confirm" => "本当に削除して良いですか？"%>
			 	</span>
			</td>
		</tr>
		<% end %>
	</tbody>
</table>

